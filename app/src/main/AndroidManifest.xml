<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myreview">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Rating.BookStore"
            android:label="@string/title_activity_book_store"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Rating.RecentTransactionPage"
            android:label="@string/title_activity_recent_transaction_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Rating.UserMarketPage"
            android:label="@string/title_activity_user_market_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Rating.FollowingAndFollowerPage"
            android:label="@string/title_activity_following_and_follower_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Rating.LandExplorerPage" />
        <activity android:name=".Rating.ShppingCartPage" />
        <activity android:name=".Rating.BookShelfPage" />
        <activity android:name=".Rating.BookListViewItemHolder" />
        <activity android:name=".Rating.MyData" />
        <activity
            android:name=".Rating.UserPage"
            android:label="@string/title_activity_user_page" />
        <activity android:name=".Rating.UserPageListViewItemHolder" />
        <activity
            android:name=".Rating.RecentHousingActivities"
            android:label="@string/title_activity_recent_housing_activities"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Rating.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Rating.PinLocationActivity"
            android:label="@string/title_activity_pin_location" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <!-- <meta-data -->
        <!-- android:name="com.google.android.geo.API_KEY" -->
        <!-- /> -->
        <activity android:name=".Rating.SellBook" />
        <activity android:name=".Rating.RentOutApartment" />
        <activity android:name=".Rating.SellItemActivity" />
        <activity
            android:name=".Rating.SellItemMain"
            android:label="@string/title_activity_sell_item_main" />
        <activity android:name=".Rating.RatingMainItemHolder" />
        <activity
            android:name=".Rating.AddCategory"
            android:label="@string/title_activity_add_category" />
        <activity
            android:name=".Rating.AddItem"
            android:label="@string/title_activity_rating" />
        <activity
            android:name=".Rating.RatingMain"
            android:label="@string/title_activity_all_ratings" />
        <activity android:name=".SignUp" />
        <activity android:name=".LogIn" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>